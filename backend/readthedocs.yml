version: 2

conda:
  environment: backend/docs/environment-docs.yml

build:
   image: latest

sphinx:
  configuration: backend/docs/conf.py

python:
  version: 3.7
  install:
    - method: pip
      path: .

submodules:                                                                                                                          
    include: all